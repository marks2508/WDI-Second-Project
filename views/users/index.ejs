

<% users.forEach(user => { %>

  <div class="container-fluid">
  <div class="row">
  <div class="col-md-4">
  <div class="card">
  <div class="card-body">
  <a href="/users/<%= user.id %>"class="card-header"><%= user.username %>'s list</a>
  </div>
  </div>
  </div>
  </div>
  </div>
  <% }) %>
